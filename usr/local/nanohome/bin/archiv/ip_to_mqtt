#!/bin/bash

Server="localhost"
DB="NanoPi"
Topic="ip"
Sensor="NanoPi_IP"

IP=$(ip a | grep wlan0 | grep inet | awk '{print $2}' | rev | cut -c4- | rev)

IP="\""$IP""\"

curl -i -XPOST 'http://'$Server':8086/write?db='$DB --data-binary $Topic' '$Sensor'='"$IP"

